[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.mysqli", "php82Extensions.mbstring", "php82Extensions.curl", "php82Extensions.openssl"]

[start]
cmd = "php82 -d extension=pdo_mysql.so -d extension=pdo.so -S 0.0.0.0:$PORT -t ."